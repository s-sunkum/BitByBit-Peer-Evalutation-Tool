<% if teacher_logged_in?%>
    <% @teams.each do |t| %>
        <%= link_to t.name, projects_path(:teamID => t.id), :class => "btn btn-primary" %>
    <% end %>
<% else %>
    <% @teams.each do |t| %>
        <%= link_to t.name, projects_path(:teamID => t.id), :class => "btn btn-primary" %>
    <% end %>
<% end %>